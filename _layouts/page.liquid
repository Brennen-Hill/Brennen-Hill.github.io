---
layout: default
---
{% if page._styles or (page.toc and page.toc.sidebar) %}
  <style type="text/css">
    {{ page._styles }}

    /*
      TOC Sidebar Overrides
      1. Make TOC static on mobile (xs) screens.
      2. Force display: block to override bootstrap-toc.min.css.
    */
    @media (max-width: 767.98px) {
      #toc-sidebar {
        position: static !important;
        display: block !important;
      }
    }
  </style>
{% endif %}

<div class="post">
  {%- comment -%}
    The <header class="post-header">...
    was moved to _layouts/default.liquid
    to fix the mobile layout stacking order.
  {%- endcomment -%}

  <article>{{ content }}</article>

  {% if page.related_publications %}
    <h2>References</h2>

    <div class="publications">{% bibliography --cited_in_order %}</div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
